"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""
# @generated-id: c632d93fa68e

from .basesdk import BaseSDK
from jsonpath import JSONPath
from mollie import models, utils
from mollie._hooks import HookContext
from mollie.types import OptionalNullable, UNSET
from mollie.utils import get_security_from_env
from mollie.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, Awaitable, Dict, List, Mapping, Optional, Union


class DraftTransfers(BaseSDK):
    def create(
        self,
        *,
        idempotency_key: Optional[str] = None,
        create_draft_transfer_request: Optional[
            Union[
                models.CreateDraftTransferRequest,
                models.CreateDraftTransferRequestTypedDict,
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DraftTransferResponse:
        r"""Create draft transfer

        > 🚧 Beta feature
        >
        > This feature is currently in beta testing, and the final specification may still change.

        Creates a draft transfer. The draft transfer immediately enters `awaiting-initiation` and appears in the
        initiator's queue in Mollie Apps. It carries no legal weight and moves no funds until a human initiator
        approves it there.

        ### Test mode

        | Action | Test-mode behavior |
        |---|---|
        | Create | Always returns a synthetic draft in `awaiting-initiation`, regardless of most input values |
        | Get / List | Always returns synthetic `awaiting-initiation` draft(s), not your real data |
        | Cancel | Always returns a synthetic `declined` draft |

        There is currently no way to synthesize an `initiated` draft via test mode alone. Initiation only happens when
        a real initiator approves in Mollie Apps, which test mode doesn't touch.

        If set, this operation will use either `advanced_access_token` or `o_auth` from the global security.

        :param idempotency_key: A unique key to ensure idempotent requests. This key should be a UUID v4 string.
        :param create_draft_transfer_request:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateDraftTransferRequestRequest(
            idempotency_key=idempotency_key,
            create_draft_transfer_request=utils.get_pydantic_model(
                create_draft_transfer_request,
                Optional[models.CreateDraftTransferRequest],
            ),
        )

        req = self._build_request(
            method="POST",
            path="/v2/business-accounts/draft-transfers",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/hal+json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.create_draft_transfer_request if request is not None else None,
                False,
                True,
                "json",
                Optional[models.CreateDraftTransferRequest],
            ),
            allow_empty_value=None,
            allowed_fields=["advanced_access_token", "o_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 5000, 2, 7500), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5xx"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="create-draft-transfer",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Draft Transfers API"],
                extensions={
                    "x-readme": {
                        "code-samples": [
                            {
                                "code": 'curl -X POST https://api.mollie.com/v2/business-accounts/draft-transfers \\\n    -H "Authorization: Bearer access_dHar4XY7LxsDOtmnkVtjNVWXLSlXsM" \\\n    -H "Content-Type: application/json" \\\n    -d \'{\n          "creditor": {\n            "fullName": "Jan Jansen",\n            "account": { "iban": "NL02ABNA0123456789" }\n          },\n          "amount": { "currency": "EUR", "value": "100.00" },\n          "description": "Invoice 12345",\n          "scheduledFor": "2025-03-01"\n        }\'',
                                "language": "shell",
                            },
                            {"code": "No example available.", "language": "php"},
                            {"code": "No example available.", "language": "node"},
                            {"code": "No example available.", "language": "python"},
                            {"code": "No example available.", "language": "ruby"},
                        ]
                    }
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "201", "application/hal+json"):
            return unmarshal_json_response(models.DraftTransferResponse, http_res)
        if utils.match_response(http_res, ["422", "429"], "application/hal+json"):
            response_data = unmarshal_json_response(models.ErrorResponseData, http_res)
            raise models.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def create_async(
        self,
        *,
        idempotency_key: Optional[str] = None,
        create_draft_transfer_request: Optional[
            Union[
                models.CreateDraftTransferRequest,
                models.CreateDraftTransferRequestTypedDict,
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DraftTransferResponse:
        r"""Create draft transfer

        > 🚧 Beta feature
        >
        > This feature is currently in beta testing, and the final specification may still change.

        Creates a draft transfer. The draft transfer immediately enters `awaiting-initiation` and appears in the
        initiator's queue in Mollie Apps. It carries no legal weight and moves no funds until a human initiator
        approves it there.

        ### Test mode

        | Action | Test-mode behavior |
        |---|---|
        | Create | Always returns a synthetic draft in `awaiting-initiation`, regardless of most input values |
        | Get / List | Always returns synthetic `awaiting-initiation` draft(s), not your real data |
        | Cancel | Always returns a synthetic `declined` draft |

        There is currently no way to synthesize an `initiated` draft via test mode alone. Initiation only happens when
        a real initiator approves in Mollie Apps, which test mode doesn't touch.

        If set, this operation will use either `advanced_access_token` or `o_auth` from the global security.

        :param idempotency_key: A unique key to ensure idempotent requests. This key should be a UUID v4 string.
        :param create_draft_transfer_request:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateDraftTransferRequestRequest(
            idempotency_key=idempotency_key,
            create_draft_transfer_request=utils.get_pydantic_model(
                create_draft_transfer_request,
                Optional[models.CreateDraftTransferRequest],
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/business-accounts/draft-transfers",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/hal+json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.create_draft_transfer_request if request is not None else None,
                False,
                True,
                "json",
                Optional[models.CreateDraftTransferRequest],
            ),
            allow_empty_value=None,
            allowed_fields=["advanced_access_token", "o_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 5000, 2, 7500), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5xx"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="create-draft-transfer",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Draft Transfers API"],
                extensions={
                    "x-readme": {
                        "code-samples": [
                            {
                                "code": 'curl -X POST https://api.mollie.com/v2/business-accounts/draft-transfers \\\n    -H "Authorization: Bearer access_dHar4XY7LxsDOtmnkVtjNVWXLSlXsM" \\\n    -H "Content-Type: application/json" \\\n    -d \'{\n          "creditor": {\n            "fullName": "Jan Jansen",\n            "account": { "iban": "NL02ABNA0123456789" }\n          },\n          "amount": { "currency": "EUR", "value": "100.00" },\n          "description": "Invoice 12345",\n          "scheduledFor": "2025-03-01"\n        }\'',
                                "language": "shell",
                            },
                            {"code": "No example available.", "language": "php"},
                            {"code": "No example available.", "language": "node"},
                            {"code": "No example available.", "language": "python"},
                            {"code": "No example available.", "language": "ruby"},
                        ]
                    }
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "201", "application/hal+json"):
            return unmarshal_json_response(models.DraftTransferResponse, http_res)
        if utils.match_response(http_res, ["422", "429"], "application/hal+json"):
            response_data = unmarshal_json_response(models.ErrorResponseData, http_res)
            raise models.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def list(
        self,
        *,
        from_: OptionalNullable[str] = UNSET,
        limit: OptionalNullable[int] = UNSET,
        status: Optional[models.DraftTransferStatus] = None,
        source: Optional[models.DraftTransferSource] = None,
        idempotency_key: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        url_override: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.ListDraftTransfersResponse]:
        r"""List draft transfers

        > 🚧 Beta feature
        >
        > This feature is currently in beta testing, and the final specification may still change.

        Retrieves a list of draft transfers for the organization including ones created in Mollie Apps, not just
        ones created via this API.

        The results are paginated.

        In test mode, this always returns a synthetic `awaiting-initiation` list. See [Create draft
        transfer](create-draft-transfer) for full test-mode behavior.

        If set, this operation will use either `advanced_access_token` or `o_auth` from the global security.

        :param from_: Provide an ID to start the result set from the item with the given ID and onwards. This allows you to paginate the
            result set.
        :param limit: The maximum number of items to return. Defaults to 50 items.
        :param status: Filter the list by draft transfer status. Omit to return draft transfers in any status.
        :param source: Filter the list by origin: `api` for draft transfers created via this API, `mollie-app` for ones created
            in Mollie Apps. Omit to return draft transfers from any source.
        :param idempotency_key: A unique key to ensure idempotent requests. This key should be a UUID v4 string.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.ListDraftTransfersRequest(
            from_=from_,
            limit=limit,
            status=status,
            source=source,
            idempotency_key=idempotency_key,
        )

        req = self._build_request(
            method="GET",
            path="/v2/business-accounts/draft-transfers",
            base_url=base_url,
            url_variables=url_variables,
            url_override=url_override,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/hal+json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["advanced_access_token", "o_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 5000, 2, 7500), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5xx"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="list-draft-transfers",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Draft Transfers API"],
                extensions={
                    "x-readme": {
                        "code-samples": [
                            {
                                "code": 'curl -X GET https://api.mollie.com/v2/business-accounts/draft-transfers \\\n    -H "Authorization: Bearer access_dHar4XY7LxsDOtmnkVtjNVWXLSlXsM"',
                                "language": "shell",
                            },
                            {"code": "No example available.", "language": "php"},
                            {"code": "No example available.", "language": "node"},
                            {"code": "No example available.", "language": "python"},
                            {"code": "No example available.", "language": "ruby"},
                        ]
                    }
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        def next_func() -> Optional[models.ListDraftTransfersResponse]:
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            next_url_match = JSONPath("$._links.next.href").parse(body)
            if len(next_url_match) == 0:
                return None

            next_url = next_url_match[0]
            if not next_url:
                return None

            if next_url[0] == "/":
                url = self._get_url(base_url, url_variables)
                next_url = url + next_url

            return self.list(
                from_=from_,
                limit=limit,
                status=status,
                source=source,
                idempotency_key=idempotency_key,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
                url_override=next_url,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/hal+json"):
            return models.ListDraftTransfersResponse(
                result=unmarshal_json_response(
                    models.ListDraftTransfersResponseBody, http_res
                ),
                next=next_func,
            )
        if utils.match_response(http_res, ["400", "429"], "application/hal+json"):
            response_data = unmarshal_json_response(models.ErrorResponseData, http_res)
            raise models.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def list_async(
        self,
        *,
        from_: OptionalNullable[str] = UNSET,
        limit: OptionalNullable[int] = UNSET,
        status: Optional[models.DraftTransferStatus] = None,
        source: Optional[models.DraftTransferSource] = None,
        idempotency_key: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        url_override: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> Optional[models.ListDraftTransfersResponse]:
        r"""List draft transfers

        > 🚧 Beta feature
        >
        > This feature is currently in beta testing, and the final specification may still change.

        Retrieves a list of draft transfers for the organization including ones created in Mollie Apps, not just
        ones created via this API.

        The results are paginated.

        In test mode, this always returns a synthetic `awaiting-initiation` list. See [Create draft
        transfer](create-draft-transfer) for full test-mode behavior.

        If set, this operation will use either `advanced_access_token` or `o_auth` from the global security.

        :param from_: Provide an ID to start the result set from the item with the given ID and onwards. This allows you to paginate the
            result set.
        :param limit: The maximum number of items to return. Defaults to 50 items.
        :param status: Filter the list by draft transfer status. Omit to return draft transfers in any status.
        :param source: Filter the list by origin: `api` for draft transfers created via this API, `mollie-app` for ones created
            in Mollie Apps. Omit to return draft transfers from any source.
        :param idempotency_key: A unique key to ensure idempotent requests. This key should be a UUID v4 string.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.ListDraftTransfersRequest(
            from_=from_,
            limit=limit,
            status=status,
            source=source,
            idempotency_key=idempotency_key,
        )

        req = self._build_request_async(
            method="GET",
            path="/v2/business-accounts/draft-transfers",
            base_url=base_url,
            url_variables=url_variables,
            url_override=url_override,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/hal+json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["advanced_access_token", "o_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 5000, 2, 7500), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5xx"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="list-draft-transfers",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Draft Transfers API"],
                extensions={
                    "x-readme": {
                        "code-samples": [
                            {
                                "code": 'curl -X GET https://api.mollie.com/v2/business-accounts/draft-transfers \\\n    -H "Authorization: Bearer access_dHar4XY7LxsDOtmnkVtjNVWXLSlXsM"',
                                "language": "shell",
                            },
                            {"code": "No example available.", "language": "php"},
                            {"code": "No example available.", "language": "node"},
                            {"code": "No example available.", "language": "python"},
                            {"code": "No example available.", "language": "ruby"},
                        ]
                    }
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        def next_func() -> Awaitable[Optional[models.ListDraftTransfersResponse]]:
            body = utils.unmarshal_json(http_res.text, Union[Dict[Any, Any], List[Any]])

            async def empty_result():
                return None

            next_url_match = JSONPath("$._links.next.href").parse(body)
            if len(next_url_match) == 0:
                return empty_result()

            next_url = next_url_match[0]
            if not next_url:
                return empty_result()

            if next_url[0] == "/":
                url = self._get_url(base_url, url_variables)
                next_url = url + next_url

            return self.list_async(
                from_=from_,
                limit=limit,
                status=status,
                source=source,
                idempotency_key=idempotency_key,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
                url_override=next_url,
            )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/hal+json"):
            return models.ListDraftTransfersResponse(
                result=unmarshal_json_response(
                    models.ListDraftTransfersResponseBody, http_res
                ),
                next=next_func,
            )
        if utils.match_response(http_res, ["400", "429"], "application/hal+json"):
            response_data = unmarshal_json_response(models.ErrorResponseData, http_res)
            raise models.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def get(
        self,
        *,
        draft_transfer_id: str,
        idempotency_key: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DraftTransferResponse:
        r"""Get draft transfer

        > 🚧 Beta feature
        >
        > This feature is currently in beta testing, and the final specification may still change.

        Retrieves a single draft transfer by its identifier.

        Only draft transfers created via this API are visible via this endpoint. Draft transfers created in Mollie
        Apps return a `404`, even though they appear in the [list endpoint](list-draft-transfers).

        In test mode, this always returns a synthetic `awaiting-initiation` draft. See [Create draft
        transfer](create-draft-transfer) for full test-mode behavior.

        If set, this operation will use either `advanced_access_token` or `o_auth` from the global security.

        :param draft_transfer_id: Provide the ID of the related draft transfer.
        :param idempotency_key: A unique key to ensure idempotent requests. This key should be a UUID v4 string.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetDraftTransferRequest(
            draft_transfer_id=draft_transfer_id,
            idempotency_key=idempotency_key,
        )

        req = self._build_request(
            method="GET",
            path="/v2/business-accounts/draft-transfers/{draftTransferId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/hal+json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["advanced_access_token", "o_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 5000, 2, 7500), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5xx"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-draft-transfer",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Draft Transfers API"],
                extensions={
                    "x-readme": {
                        "code-samples": [
                            {
                                "code": 'curl -X GET https://api.mollie.com/v2/business-accounts/draft-transfers/badrt_87GByBuj4UCcUTEbs6aGJ \\\n    -H "Authorization: Bearer access_dHar4XY7LxsDOtmnkVtjNVWXLSlXsM"',
                                "language": "shell",
                            },
                            {"code": "No example available.", "language": "php"},
                            {"code": "No example available.", "language": "node"},
                            {"code": "No example available.", "language": "python"},
                            {"code": "No example available.", "language": "ruby"},
                        ]
                    }
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/hal+json"):
            return unmarshal_json_response(models.DraftTransferResponse, http_res)
        if utils.match_response(http_res, ["404", "429"], "application/hal+json"):
            response_data = unmarshal_json_response(models.ErrorResponseData, http_res)
            raise models.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def get_async(
        self,
        *,
        draft_transfer_id: str,
        idempotency_key: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DraftTransferResponse:
        r"""Get draft transfer

        > 🚧 Beta feature
        >
        > This feature is currently in beta testing, and the final specification may still change.

        Retrieves a single draft transfer by its identifier.

        Only draft transfers created via this API are visible via this endpoint. Draft transfers created in Mollie
        Apps return a `404`, even though they appear in the [list endpoint](list-draft-transfers).

        In test mode, this always returns a synthetic `awaiting-initiation` draft. See [Create draft
        transfer](create-draft-transfer) for full test-mode behavior.

        If set, this operation will use either `advanced_access_token` or `o_auth` from the global security.

        :param draft_transfer_id: Provide the ID of the related draft transfer.
        :param idempotency_key: A unique key to ensure idempotent requests. This key should be a UUID v4 string.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetDraftTransferRequest(
            draft_transfer_id=draft_transfer_id,
            idempotency_key=idempotency_key,
        )

        req = self._build_request_async(
            method="GET",
            path="/v2/business-accounts/draft-transfers/{draftTransferId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/hal+json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["advanced_access_token", "o_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 5000, 2, 7500), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5xx"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-draft-transfer",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Draft Transfers API"],
                extensions={
                    "x-readme": {
                        "code-samples": [
                            {
                                "code": 'curl -X GET https://api.mollie.com/v2/business-accounts/draft-transfers/badrt_87GByBuj4UCcUTEbs6aGJ \\\n    -H "Authorization: Bearer access_dHar4XY7LxsDOtmnkVtjNVWXLSlXsM"',
                                "language": "shell",
                            },
                            {"code": "No example available.", "language": "php"},
                            {"code": "No example available.", "language": "node"},
                            {"code": "No example available.", "language": "python"},
                            {"code": "No example available.", "language": "ruby"},
                        ]
                    }
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/hal+json"):
            return unmarshal_json_response(models.DraftTransferResponse, http_res)
        if utils.match_response(http_res, ["404", "429"], "application/hal+json"):
            response_data = unmarshal_json_response(models.ErrorResponseData, http_res)
            raise models.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def cancel(
        self,
        *,
        draft_transfer_id: str,
        idempotency_key: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DraftTransferResponse:
        r"""Cancel draft transfer

        > 🚧 Beta feature
        >
        > This feature is currently in beta testing, and the final specification may still change.

        Cancels a draft transfer created via this API. Transitions the draft transfer to `declined` with
        `statusReason` set to `\"Declined by creator\"`.

        Only draft transfers created via this API, and still in `awaiting-initiation`, can be cancelled this way. A
        `422` is returned if the initiator has already started approving it.

        In test mode, this always returns a synthetic `declined` draft. See [Create draft
        transfer](create-draft-transfer) for full test-mode behavior.

        If set, this operation will use either `advanced_access_token` or `o_auth` from the global security.

        :param draft_transfer_id: Provide the ID of the related draft transfer.
        :param idempotency_key: A unique key to ensure idempotent requests. This key should be a UUID v4 string.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CancelDraftTransferRequest(
            draft_transfer_id=draft_transfer_id,
            idempotency_key=idempotency_key,
        )

        req = self._build_request(
            method="DELETE",
            path="/v2/business-accounts/draft-transfers/{draftTransferId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/hal+json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["advanced_access_token", "o_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 5000, 2, 7500), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5xx"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="cancel-draft-transfer",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Draft Transfers API"],
                extensions={
                    "x-readme": {
                        "code-samples": [
                            {
                                "code": 'curl -X DELETE https://api.mollie.com/v2/business-accounts/draft-transfers/badrt_87GByBuj4UCcUTEbs6aGJ \\\n    -H "Authorization: Bearer access_dHar4XY7LxsDOtmnkVtjNVWXLSlXsM"',
                                "language": "shell",
                            },
                            {"code": "No example available.", "language": "php"},
                            {"code": "No example available.", "language": "node"},
                            {"code": "No example available.", "language": "python"},
                            {"code": "No example available.", "language": "ruby"},
                        ]
                    }
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/hal+json"):
            return unmarshal_json_response(models.DraftTransferResponse, http_res)
        if utils.match_response(
            http_res, ["404", "422", "429"], "application/hal+json"
        ):
            response_data = unmarshal_json_response(models.ErrorResponseData, http_res)
            raise models.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def cancel_async(
        self,
        *,
        draft_transfer_id: str,
        idempotency_key: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DraftTransferResponse:
        r"""Cancel draft transfer

        > 🚧 Beta feature
        >
        > This feature is currently in beta testing, and the final specification may still change.

        Cancels a draft transfer created via this API. Transitions the draft transfer to `declined` with
        `statusReason` set to `\"Declined by creator\"`.

        Only draft transfers created via this API, and still in `awaiting-initiation`, can be cancelled this way. A
        `422` is returned if the initiator has already started approving it.

        In test mode, this always returns a synthetic `declined` draft. See [Create draft
        transfer](create-draft-transfer) for full test-mode behavior.

        If set, this operation will use either `advanced_access_token` or `o_auth` from the global security.

        :param draft_transfer_id: Provide the ID of the related draft transfer.
        :param idempotency_key: A unique key to ensure idempotent requests. This key should be a UUID v4 string.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CancelDraftTransferRequest(
            draft_transfer_id=draft_transfer_id,
            idempotency_key=idempotency_key,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/v2/business-accounts/draft-transfers/{draftTransferId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/hal+json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["advanced_access_token", "o_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(500, 5000, 2, 7500), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "5xx"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="cancel-draft-transfer",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Draft Transfers API"],
                extensions={
                    "x-readme": {
                        "code-samples": [
                            {
                                "code": 'curl -X DELETE https://api.mollie.com/v2/business-accounts/draft-transfers/badrt_87GByBuj4UCcUTEbs6aGJ \\\n    -H "Authorization: Bearer access_dHar4XY7LxsDOtmnkVtjNVWXLSlXsM"',
                                "language": "shell",
                            },
                            {"code": "No example available.", "language": "php"},
                            {"code": "No example available.", "language": "node"},
                            {"code": "No example available.", "language": "python"},
                            {"code": "No example available.", "language": "ruby"},
                        ]
                    }
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/hal+json"):
            return unmarshal_json_response(models.DraftTransferResponse, http_res)
        if utils.match_response(
            http_res, ["404", "422", "429"], "application/hal+json"
        ):
            response_data = unmarshal_json_response(models.ErrorResponseData, http_res)
            raise models.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)
