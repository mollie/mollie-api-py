"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""
# @generated-id: 206462761a2c

from __future__ import annotations
from .url import URL, URLTypedDict
from enum import Enum
from mollie import models, utils
from mollie.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class GiftcardStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The status of the issuer.
    If the status is `pending-issuer`, an additional action from your side may be required with the issuer.
    """

    ACTIVATED = "activated"
    PENDING_ISSUER = "pending-issuer"


class GiftcardLinksTypedDict(TypedDict):
    r"""An object with several relevant URLs. Every URL object will contain an `href` and a `type` field."""

    self_: NotRequired[URLTypedDict]
    r"""In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field."""
    documentation: NotRequired[URLTypedDict]
    r"""In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field."""


class GiftcardLinks(BaseModel):
    r"""An object with several relevant URLs. Every URL object will contain an `href` and a `type` field."""

    self_: Annotated[Optional[URL], pydantic.Field(alias="self")] = None
    r"""In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field."""

    documentation: Optional[URL] = None
    r"""In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["self", "documentation"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class GiftcardTypedDict(TypedDict):
    resource: str
    r"""Indicates the response contains a payment method issuer object. Will always contain the string `issuer` for this
    endpoint.
    """
    id: str
    r"""The unique identifier of the payment method issuer."""
    description: str
    r"""The full name of the payment method issuer."""
    status: GiftcardStatus
    links: GiftcardLinksTypedDict
    r"""An object with several relevant URLs. Every URL object will contain an `href` and a `type` field."""


class Giftcard(BaseModel):
    resource: str
    r"""Indicates the response contains a payment method issuer object. Will always contain the string `issuer` for this
    endpoint.
    """

    id: str
    r"""The unique identifier of the payment method issuer."""

    description: str
    r"""The full name of the payment method issuer."""

    status: GiftcardStatus

    links: Annotated[GiftcardLinks, pydantic.Field(alias="_links")]
    r"""An object with several relevant URLs. Every URL object will contain an `href` and a `type` field."""

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return models.GiftcardStatus(value)
            except ValueError:
                return value
        return value


try:
    GiftcardLinks.model_rebuild()
except NameError:
    pass
try:
    Giftcard.model_rebuild()
except NameError:
    pass
