"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""
# @generated-id: 761fd37f9f16

from __future__ import annotations
from .oauth_grant_type import OauthGrantType
from mollie.types import BaseModel, UNSET_SENTINEL
from mollie.utils import FieldMetadata, HeaderMetadata, RequestMetadata
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


OAUTH_GENERATE_TOKENS_OP_SERVERS = [
    "https://api.mollie.com/oauth2",
]


class OauthGenerateTokensRequestBodyTypedDict(TypedDict):
    grant_type: OauthGrantType
    code: NotRequired[str]
    r"""The authorization code you received when creating the authorization. Only use this field when using
    grant type `authorization_code`.
    """
    refresh_token: NotRequired[str]
    r"""The refresh token you received when creating the authorization. Only use this field when using grant
    type `refresh_token`.
    """
    redirect_uri: NotRequired[str]
    r"""The URL the merchant is sent back to once the request has been authorized. It must match the URL you set
    when registering your app.

    For consecutive refresh token requests, this parameter is required only if the initial authorization
    code grant request also contained a `redirect_uri`.
    """


class OauthGenerateTokensRequestBody(BaseModel):
    grant_type: OauthGrantType

    code: Optional[str] = None
    r"""The authorization code you received when creating the authorization. Only use this field when using
    grant type `authorization_code`.
    """

    refresh_token: Optional[str] = None
    r"""The refresh token you received when creating the authorization. Only use this field when using grant
    type `refresh_token`.
    """

    redirect_uri: Optional[str] = None
    r"""The URL the merchant is sent back to once the request has been authorized. It must match the URL you set
    when registering your app.

    For consecutive refresh token requests, this parameter is required only if the initial authorization
    code grant request also contained a `redirect_uri`.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["code", "refresh_token", "redirect_uri"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class OauthGenerateTokensRequestTypedDict(TypedDict):
    idempotency_key: NotRequired[str]
    r"""A unique key to ensure idempotent requests. This key should be a UUID v4 string."""
    request_body: NotRequired[OauthGenerateTokensRequestBodyTypedDict]


class OauthGenerateTokensRequest(BaseModel):
    idempotency_key: Annotated[
        Optional[str],
        pydantic.Field(alias="idempotency-key"),
        FieldMetadata(header=HeaderMetadata(style="simple", explode=False)),
    ] = None
    r"""A unique key to ensure idempotent requests. This key should be a UUID v4 string."""

    request_body: Annotated[
        Optional[OauthGenerateTokensRequestBody],
        FieldMetadata(request=RequestMetadata(media_type="application/json")),
    ] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["idempotency-key", "RequestBody"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class OauthGenerateTokensResponseTypedDict(TypedDict):
    r"""The newly generated access token and refresh token."""

    access_token: NotRequired[str]
    r"""The app access token, with which you will be able to access the Mollie API on the merchant's behalf."""
    refresh_token: NotRequired[str]
    r"""The refresh token, with which you will be able to retrieve new app access tokens on this endpoint. The
    refresh token does not expire.
    """
    expires_in: NotRequired[int]
    r"""The number of seconds left before the app access token expires. Be sure to renew your app access token
    before this reaches zero.
    """
    token_type: NotRequired[str]
    r"""As per OAuth standards, the provided app access token can only be used with `bearer` authentication.

    Possible values: `bearer`
    """
    scope: NotRequired[str]
    r"""A space-separated list of [permissions](https://docs.mollie.com/docs/permissions)."""


class OauthGenerateTokensResponse(BaseModel):
    r"""The newly generated access token and refresh token."""

    access_token: Optional[str] = None
    r"""The app access token, with which you will be able to access the Mollie API on the merchant's behalf."""

    refresh_token: Optional[str] = None
    r"""The refresh token, with which you will be able to retrieve new app access tokens on this endpoint. The
    refresh token does not expire.
    """

    expires_in: Optional[int] = None
    r"""The number of seconds left before the app access token expires. Be sure to renew your app access token
    before this reaches zero.
    """

    token_type: Optional[str] = None
    r"""As per OAuth standards, the provided app access token can only be used with `bearer` authentication.

    Possible values: `bearer`
    """

    scope: Optional[str] = None
    r"""A space-separated list of [permissions](https://docs.mollie.com/docs/permissions)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["access_token", "refresh_token", "expires_in", "token_type", "scope"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
