"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""
# @generated-id: 2627f08cba9f

from __future__ import annotations
from .url import URL, URLTypedDict
from enum import Enum
from mollie import models, utils
from mollie.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class VoucherImageTypedDict(TypedDict):
    r"""URLs of images representing the payment method issuer."""

    size1x: NotRequired[str]
    r"""The URL pointing to an icon of 32 by 24 pixels."""
    size2x: NotRequired[str]
    r"""The URL pointing to an icon of 64 by 48 pixels."""
    svg: NotRequired[str]
    r"""The URL pointing to a vector version of the icon. Usage of this format is preferred, since the icon can
    scale to any desired size without compromising visual quality.
    """


class VoucherImage(BaseModel):
    r"""URLs of images representing the payment method issuer."""

    size1x: Optional[str] = None
    r"""The URL pointing to an icon of 32 by 24 pixels."""

    size2x: Optional[str] = None
    r"""The URL pointing to an icon of 64 by 48 pixels."""

    svg: Optional[str] = None
    r"""The URL pointing to a vector version of the icon. Usage of this format is preferred, since the icon can
    scale to any desired size without compromising visual quality.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["size1x", "size2x", "svg"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class VoucherStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The status of the issuer.
    If the status is `pending-issuer`, an additional action from your side may be required with the issuer.
    """

    ACTIVATED = "activated"
    PENDING_ISSUER = "pending-issuer"


class ContractorTypedDict(TypedDict):
    r"""Information regarding the *contractor*. Only relevant for `voucher` issuers."""

    id: NotRequired[str]
    name: NotRequired[str]
    contract_id: NotRequired[str]


class Contractor(BaseModel):
    r"""Information regarding the *contractor*. Only relevant for `voucher` issuers."""

    id: Optional[str] = None

    name: Optional[str] = None

    contract_id: Annotated[Optional[str], pydantic.Field(alias="contractId")] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id", "name", "contractId"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class VoucherLinksTypedDict(TypedDict):
    r"""An object with several relevant URLs. Every URL object will contain an `href` and a `type` field."""

    self_: NotRequired[URLTypedDict]
    r"""In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field."""
    documentation: NotRequired[URLTypedDict]
    r"""In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field."""


class VoucherLinks(BaseModel):
    r"""An object with several relevant URLs. Every URL object will contain an `href` and a `type` field."""

    self_: Annotated[Optional[URL], pydantic.Field(alias="self")] = None
    r"""In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field."""

    documentation: Optional[URL] = None
    r"""In v2 endpoints, URLs are commonly represented as objects with an `href` and `type` field."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["self", "documentation"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class VoucherTypedDict(TypedDict):
    resource: str
    r"""Indicates the response contains a payment method issuer object. Will always contain the string `issuer` for this
    endpoint.
    """
    id: str
    r"""The unique identifier of the payment method issuer."""
    name: str
    r"""The full name of the payment method issuer."""
    image: VoucherImageTypedDict
    r"""URLs of images representing the payment method issuer."""
    status: VoucherStatus
    contractor: ContractorTypedDict
    r"""Information regarding the *contractor*. Only relevant for `voucher` issuers."""
    links: VoucherLinksTypedDict
    r"""An object with several relevant URLs. Every URL object will contain an `href` and a `type` field."""


class Voucher(BaseModel):
    resource: str
    r"""Indicates the response contains a payment method issuer object. Will always contain the string `issuer` for this
    endpoint.
    """

    id: str
    r"""The unique identifier of the payment method issuer."""

    name: str
    r"""The full name of the payment method issuer."""

    image: VoucherImage
    r"""URLs of images representing the payment method issuer."""

    status: VoucherStatus

    contractor: Contractor
    r"""Information regarding the *contractor*. Only relevant for `voucher` issuers."""

    links: Annotated[VoucherLinks, pydantic.Field(alias="_links")]
    r"""An object with several relevant URLs. Every URL object will contain an `href` and a `type` field."""

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return models.VoucherStatus(value)
            except ValueError:
                return value
        return value


try:
    Contractor.model_rebuild()
except NameError:
    pass
try:
    VoucherLinks.model_rebuild()
except NameError:
    pass
try:
    Voucher.model_rebuild()
except NameError:
    pass
