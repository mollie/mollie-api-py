# Action

The action performed on the unmatched credit transfer.

## Example Usage

```python
from mollie.models import Action

value = Action.MATCH

# Open enum: unrecognized values are captured as UnrecognizedStr
```


## Values

| Name     | Value    |
| -------- | -------- |
| `MATCH`  | match    |
| `RETURN` | return   |