# AccountNumberFormatResponse

The format of the account number.

## Example Usage

```python
from mollie.models import AccountNumberFormatResponse

value = AccountNumberFormatResponse.IBAN

# Open enum: unrecognized values are captured as UnrecognizedStr
```


## Values

| Name   | Value  |
| ------ | ------ |
| `IBAN` | iban   |