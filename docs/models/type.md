# Type

The type of source. Currently only the source type `organization` is supported.

## Example Usage

```python
from mollie.models import Type

value = Type.ORGANIZATION
```


## Values

| Name           | Value          |
| -------------- | -------------- |
| `ORGANIZATION` | organization   |